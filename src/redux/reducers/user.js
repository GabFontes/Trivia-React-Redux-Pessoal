const INITAL_STATE = {
  name: '',
  assertions: '',
  score: '',
  gravatarEmail: '',
};

function user(state = INITAL_STATE, { type }) {
  switch (type) {
  default:
    return state;
  }
}

export default user;
